This is a backup of basically everything for my dev machine

It uses gnu-stow to create symlinks for all the files allowing easy backup (I need to automate the backup process) 

the setup directory is ignored by gnu-stow and is where all other setup related stuff can go 
